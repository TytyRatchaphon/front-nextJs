meta {
  name: /user/event
  type: http
  seq: 1
}

get {
  url: {{path}}/user/event
  body: none
  auth: inherit
}

headers {
  Authorization: {{token}}
}

settings {
  encodeUrl: true
}

docs {
  - ใช้เรียกข้อมูลหน้ากิจกรรม
      
  - มีข้อมูล 2 ชุด คือ ของ กิจกรรมยอดใช้เหรียญสะสม และ กิจกรรมยอดเติมเหรียญสะสมรายอาทิตย์
}
