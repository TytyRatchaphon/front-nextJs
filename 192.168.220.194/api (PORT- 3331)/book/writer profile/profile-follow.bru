meta {
  name: /profile/follow
  type: http
  seq: 2
}

post {
  url: {{path}}/profile/follow
  body: json
  auth: inherit
}

headers {
  Authorization: {{token}}
}

body:json {
  {
      "writer_id":53,
      "action": "follow"
  }
}

settings {
  encodeUrl: true
}
